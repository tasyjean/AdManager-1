# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /    		                    controllers.Application.index()
GET     /home                           controllers.frontend.StaticPages.home()

GET     /contact                   		controllers.frontend.StaticPages.contact()
GET     /registration        			controllers.frontend.StaticPages.registration()
GET     /help                    		controllers.frontend.StaticPages.help()
GET     /about		        			controllers.frontend.StaticPages.about()
GET     /login                   		controllers.frontend.StaticPages.login()
GET 	/dashboard						controllers.backend.Backend.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
