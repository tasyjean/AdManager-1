# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /    		                    controllers.Application.index()
GET     /home                           controllers.frontend.Frontend.home()

GET     /contact                   		controllers.frontend.Frontend.contact()
GET     /registration        			controllers.frontend.Frontend.registration()
GET     /help                    		controllers.frontend.Frontend.help()
GET     /about		        			controllers.frontend.Frontend.about()
GET     /login                   		controllers.frontend.Frontend.login()
GET 	/dashboard						controllers.backend.Backend.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
